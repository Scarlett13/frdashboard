{
  /* <button
        className="ms-4 w-0"
        onClick={() => setCollapsed((prev: boolean) => !prev)}
      >
        <VscThreeBars className="w-8 h-8 mt-10 hover:scale-110" />
      </button> */
}
//   {!collapsed && (<></>

//   )}
